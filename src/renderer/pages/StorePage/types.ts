export type StoreData = {
  storeNumber: string;
  storeName: string;
  owner: string;
  address: string;
};


export type Inputs = {
  storeNumber: string;
  storeName: string;
  owner: string;
  address: string;
  name: string;
  clicked: boolean;
  page: number;
}
